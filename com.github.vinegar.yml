# Modified from net.brinkervii.grapejuice, thanks!

app-id: com.github.vinegar
runtime: org.freedesktop.Platform
runtime-version: '22.08'
base: org.winehq.Wine
base-version: stable-22.08
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang
command: vinegar

finish-args:
  - --share=network
  - --socket=x11 # Will be removed or replaced with fallback-x11 once Wine is improved
  - --share=ipc
  - --socket=wayland
  #- --device=dri # Uncomment if --device=all is removed/commented to fix graphics issues
  - --allow=devel # Necessary for VMProtect to function.
  - --socket=pulseaudio
  - --allow=multiarch
  - --device=all # Necessary for controller support (important on Steam Deck)

build-options:
  env:
    - GOBIN=/app/bin
    - GOROOT=/usr/lib/sdk/golang

add-extensions:
  org.freedesktop.Platform.Compat.i386:
    directory: lib/i386-linux-gnu
    version: '22.08'

  org.freedesktop.Platform.GL32:
    directory: lib/i386-linux-gnu/GL
    version: '1.4'
    versions: 22.08;1.4
    subdirectories: true
    no-autodownload: true
    autodelete: false
    add-ld-path: lib
    merge-dirs: vulkan/icd.d;glvnd/egl_vendor.d;OpenCL/vendors;lib/dri;lib/d3d;vulkan/explicit_layer.d;vulkan/implicit_layer.d
    download-if: active-gl-driver
    enable-if: active-gl-driver

modules:
  - name: bundle-setup
    buildsystem: simple
    build-commands:
      - mkdir -p /app/lib32
      - mkdir -p /app/lib/i386-linux-gnu
      - install -Dm644 ld.so.conf -t /app/etc/
    sources:
      - type: file
        path: ld.so.conf

  - name: vinegar
    buildsystem: simple
    build-commands:
      - $GOROOT/bin/go build -ldflags="-s -w" -buildvcs=false -o vinegar
      - install -Dm755 vinegar /app/bin/vinegar
      - install -Dm644 desktop/app.desktop /app/share/applications/com.github.vinegar.app.desktop  
      - install -Dm644 desktop/player.desktop /app/share/applications/com.github.vinegar.roblox.player.desktop
      - install -Dm644 desktop/studio.desktop /app/share/applications/com.github.vinegar.roblox.studio.desktop
        #      - install -Dm644 icons/64/player.png /usr/local/share/icons/hicolor/64x64/apps/vinegar-roblox-player.png
        #      - install -Dm644 icons/128/player.png /usr/local/share/icons/hicolor/128x128/apps/vinegar-roblox-player.png
        #      - install -Dm644 icons/64/studio.png /usr/local/share/icons/hicolor/64x64/apps/vinegar-roblox-studio.png
        #      - install -Dm644 icons/128/studio.png /usr/local/share/icons/hicolor/128x128/apps/vinegar-roblox-studio.png
      - install -Dm644 com.github.vinegar.metainfo.xml -t /app/share/metainfo
    sources:
      - type: git
        url: https://github.com/vinegar-dev/vinegar.git
        tag: v1.1.1
        commit: af7f1de714c5a78111e78399604f51d9744375e7

      - type: file
        path: com.github.vinegar.metainfo.xml
